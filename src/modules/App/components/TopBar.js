import React from 'react';
import PropTypes from 'prop-types';
import styles from './topBar.module.scss';

const TopBar = props => <nav className={styles.self}>navBar</nav>;

TopBar.propTypes = {};

export default TopBar;
